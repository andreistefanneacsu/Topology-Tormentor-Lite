[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "topology_tormentor"
version = "0.1.0"
description = "A network topology simulator"
dependencies = []

[tool.setuptools]
packages = ["Devices","GUI"]